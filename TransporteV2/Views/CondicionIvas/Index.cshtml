@model IEnumerable<TransporteV2.Entidades.CondicionIva>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CondicionIva1)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CondicionIva1)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdCondicionIva">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdCondicionIva">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdCondicionIva">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
