@model IEnumerable<TransporteV2.Entidades.LicenciaChofer>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.FechaVencimiento)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdChoferNavigation)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdTiposDocumentosNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.FechaVencimiento)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdChoferNavigation.IdChofer)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdTiposDocumentosNavigation.IdTiposDocumentos)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdLicenciaChofer">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdLicenciaChofer">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdLicenciaChofer">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
